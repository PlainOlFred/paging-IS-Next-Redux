// Actions
import { } from '../actions/types'

const initialState= {
  test: [],
 
}

export default (state = initialState, action) => {
  switch(action.types) {
      
    default:

      return state
  }
}